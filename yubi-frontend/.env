VITE_APP_NAME=YUBI
VITE_BASE_URL=https://www.yubi.co.in/api/
# Local API (dev). Vite dev server uses this for axios; use /api/ + proxy if you prefer same-origin.
VITE_DEV_API_URL=http://localhost:4000/api/
